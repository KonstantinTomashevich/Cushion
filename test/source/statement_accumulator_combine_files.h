#pragma once

CUSHION_STATEMENT_ACCUMULATOR_PUSH (my_data, optional, unordered) { int x; }
CUSHION_STATEMENT_ACCUMULATOR_PUSH (my_data, optional, unordered) { int y; }
CUSHION_STATEMENT_ACCUMULATOR_PUSH (my_data, optional, unordered) { int z; }
CUSHION_STATEMENT_ACCUMULATOR_PUSH (my_data, optional, unordered) { int w; }
