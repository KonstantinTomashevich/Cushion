// Include io library (testing that comments are omitted properly).
#include <stdio.h>

/*
 * Entry function (testing that comments are omitted).
 * Let's add more comments here.
 * And more lines!
 * And even more lines!
 */
int main (int argc, char **argv)
{
    /* Another comment (testing that comments are omitted). */
    printf ("Hello, world!\n");
    return 0;
}
